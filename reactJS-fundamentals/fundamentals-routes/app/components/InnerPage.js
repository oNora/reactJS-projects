var React = require('react');

var InnerPage = React.createClass({
    render: function() {
        return (
            <div> Hallo from InnerPage! </div>
        )
    }
});

module.exports = InnerPage;